resources:
  accelerators: H100:1
  use_spot: true
  disk_size: 256

file_mounts:
  /data:
    name: vedaangh-voice-lab-data
    source: ./data
    store: s3
    mode: MOUNT

setup: |
  cd ~/sky_workdir/research
  pip install -r ../requirements.txt

run: |
  cd ~/sky_workdir/research
  echo "TODO: implement evaluation script"

envs:
  WANDB_API_KEY:
